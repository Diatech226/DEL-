import Link from 'next/link';
const money = (value, currency='XOF') => value ? `${value} ${currency}` : 'Prix sur demande';
export default function EquipmentCard({ equipment }) { return <article className="card flex flex-col gap-2"><span className="badge bg-amber-100 text-amber-800">{equipment.status}</span><h3 className="text-xl font-bold">{equipment.title}</h3><p>{equipment.category}</p><p className="text-gray-600">{equipment.brand || 'Marque N/A'} · {equipment.model || 'Modèle N/A'}</p><p>{equipment.city}, {equipment.country}</p><p><b>Location mensuelle :</b> {money(equipment.rentalPricePerMonth, equipment.currency)}</p><p><b>Vente :</b> {money(equipment.salePrice, equipment.currency)}</p><Link className="btn mt-3 bg-forest text-white text-center" href={`/equipment/${equipment._id}`}>Voir détail</Link></article> }
