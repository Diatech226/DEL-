import Link from 'next/link';import {getPublicSettings} from '../lib/api';
export default async function Footer(){let s={legalName:'DEL',contact:{}};try{s={...s,...await getPublicSettings()}}catch{}const c=s.contact||{};return <footer className="bg-coal p-8 text-center text-sm text-gray-300"><p>© 2026 {s.legalName||s.platformName||'DEL'} — {s.slogan||'Plateforme industrielle mines, BTP et logistique.'}</p><p className="mt-2">{[c.email,c.phone,c.website,c.address].filter(Boolean).join(' · ')}</p><p className="mt-3 flex justify-center gap-4"><Link href="/conditions">Conditions</Link><Link href="/confidentialite">Confidentialité</Link></p></footer>}
