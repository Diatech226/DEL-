'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {register,setToken} from '../../lib/api';
export default function Register(){const r=useRouter();const [msg,setMsg]=useState('');async function submit(e){e.preventDefault();setMsg('');try{const {token}=await register(Object.fromEntries(new FormData(e.currentTarget).entries()));setToken(token);setMsg('Compte créé avec succès.');r.push('/dashboard')}catch(err){setMsg(err.message)}}return <section className="mx-auto max-w-2xl px-6 py-12"><h1 className="text-4xl font-black">Créer un compte</h1><form onSubmit={submit} className="card mt-8 grid gap-4 md:grid-cols-2"><input className="input" name="fullName" placeholder="Nom complet" required/><input className="input" name="email" type="email" placeholder="Email"/><input className="input" name="phone" placeholder="Téléphone"/><input className="input" name="password" type="password" placeholder="Mot de passe" required minLength={6}/><select className="input" name="role" required>{['OWNER','COMPANY','INVESTOR','TECHNICIAN'].map(x=><option key={x}>{x}</option>)}</select><select className="input" name="accountType">{['INDIVIDUAL','COMPANY'].map(x=><option key={x}>{x}</option>)}</select><input className="input" name="country" placeholder="Pays"/><input className="input" name="city" placeholder="Ville"/><button className="btn bg-gold text-coal">Créer mon compte</button>{msg&&<p className={msg.includes('succès')?'alert-success':'alert-error'}>{msg}</p>}</form></section>}
