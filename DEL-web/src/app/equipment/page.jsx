'use client';import {useEffect,useState} from 'react';import {api} from '../../lib/api';import EquipmentCard from '../../components/EquipmentCard';
export default function EquipmentPage(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{api.getEquipment().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <section className="mx-auto max-w-7xl px-6 py-12"><h1 className="text-4xl font-black">Engins disponibles</h1>{loading&&<p className="mt-8">Chargement...</p>}{error&&<p className="mt-8 text-red-600">{error}</p>}{!loading&&!error&&items.length===0&&<p className="mt-8">Aucun engin disponible pour le moment.</p>}<div className="mt-8 grid gap-6 md:grid-cols-3">{items.map(e=><EquipmentCard key={e._id} equipment={e}/>)}</div></section>}
