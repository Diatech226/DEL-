'use client';import {useState} from 'react';import {createEquipmentRequest} from '../../lib/api';
export default function Ask(){const [msg,setMsg]=useState(''),[ok,setOk]=useState(false);async function submit(e){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const data=Object.fromEntries(f.entries());['driverRequired','fuelIncluded','maintenanceIncluded','insuranceRequired'].forEach(k=>data[k]=f.get(k)==='on');['quantity','durationMonths','proposedPrice'].forEach(k=>{if(data[k])data[k]=Number(data[k]);else delete data[k]});data.status='OPEN';try{await createEquipmentRequest(data);setOk(true);setMsg('Demande envoyée avec succès.');form.reset()}catch(err){setOk(false);setMsg(err.message)}}return <section className="mx-auto max-w-4xl px-6 py-12"><h1 className="text-4xl font-black">Demander des engins</h1><form onSubmit={submit} className="card mt-8 grid gap-4 md:grid-cols-2">{['companyName','contactName','contactPhone','title','equipmentCategory','quantity','country','city','workSiteLocation','durationMonths','proposedPrice','currency','priceUnit'].map(n=><input className="input" name={n} placeholder={n} required={['companyName','contactName','contactPhone','equipmentCategory','quantity','country','city'].includes(n)} key={n}/>) }<textarea className="input md:col-span-2" name="description" placeholder="description"/><div className="md:col-span-2 grid gap-3 md:grid-cols-2">{['driverRequired','fuelIncluded','maintenanceIncluded','insuranceRequired'].map(k=><label key={k}><input type="checkbox" name={k}/> {k}</label>)}</div><button className="btn bg-gold text-coal">Envoyer</button></form>{msg&&<p className={ok?'alert-success mt-4':'alert-error mt-4'}>{msg}</p>}</section>}
