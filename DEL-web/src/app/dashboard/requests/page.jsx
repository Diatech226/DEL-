'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {getMyRequests} from '../../../lib/api';
export default function MyRequests(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{getMyRequests().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <section><div className="flex items-center justify-between gap-4"><h1 className="text-3xl font-black">Mes demandes</h1><Link className="btn bg-gold text-coal" href="/demander-des-engins">Créer une nouvelle demande</Link></div>{loading&&<p className="card mt-6">Chargement…</p>}{error&&<p className="alert-error mt-6">{error}</p>}{!loading&&!error&&items.length===0&&<p className="card mt-6">Vous n’avez pas encore publié de demande d’engins.</p>}{items.length>0&&<div className="card mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left"><th>Titre</th><th>Type engin</th><th>Quantité</th><th>Ville</th><th>Prix proposé</th><th>Durée</th><th>Statut</th><th>Date création</th></tr></thead><tbody>{items.map(r=><tr className="border-t" key={r._id}><td>{r.title||r.companyName}</td><td>{r.equipmentCategory}</td><td>{r.quantity}</td><td>{r.city}</td><td>{r.proposedPrice||'N/A'} {r.currency}</td><td>{r.durationMonths||'—'} mois</td><td><span className="badge bg-purple-100 text-purple-800">{r.status}</span></td><td>{r.createdAt?new Date(r.createdAt).toLocaleDateString('fr-FR'):'—'}</td></tr>)}</tbody></table></div>}</section>}
