'use client';import {useEffect,useState} from 'react';import {getMyDocuments} from '../../../lib/api';
export default function MyDocuments(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{getMyDocuments().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <section><h1 className="text-3xl font-black">Mes documents</h1><div className="alert-success mt-6">Pour ajouter un document lié à un engin ou à une demande, utilisez la page concernée. Les documents sont vérifiés par l’équipe DEL.</div>{loading&&<p className="card mt-6">Chargement…</p>}{error&&<p className="alert-error mt-6">{error}</p>}{!loading&&!error&&items.length===0&&<p className="card mt-6">Aucun document lié à votre compte.</p>}{items.length>0&&<div className="card mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left"><th>Titre</th><th>Type</th><th>Entité liée</th><th>Statut</th><th>Lien document</th><th>Raison rejet</th></tr></thead><tbody>{items.map(d=><tr className="border-t" key={d._id}><td>{d.title}</td><td>{d.type}</td><td>{d.entityType} · {d.entityId}</td><td><span className="badge bg-blue-100 text-blue-800">{d.status}</span></td><td><a className="underline" href={d.fileUrl} target="_blank">Voir</a></td><td>{d.rejectionReason||'—'}</td></tr>)}</tbody></table></div>}</section>}
