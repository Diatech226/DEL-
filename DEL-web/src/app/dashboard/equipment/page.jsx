'use client';import DownloadPdfButton from '../../../components/DownloadPdfButton';import Link from 'next/link';import {useEffect,useState} from 'react';import {getMyEquipment} from '../../../lib/api';
export default function MyEquipment(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{getMyEquipment().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <section><div className="flex items-center justify-between gap-4"><h1 className="text-3xl font-black">Mes engins</h1><div className="flex flex-wrap gap-2"><Link className="btn bg-coal text-white" href="/dashboard/missions">Missions</Link><Link className="btn bg-coal text-white" href="/dashboard/contracts">Contrats</Link><Link className="btn bg-gold text-coal" href="/deposer-un-engin">Déposer un nouvel engin</Link></div></div>{loading&&<p className="card mt-6">Chargement…</p>}{error&&<p className="alert-error mt-6">{error}</p>}{!loading&&!error&&items.length===0&&<p className="card mt-6">Vous n’avez pas encore déposé d’engin.</p>}{items.length>0&&<div className="card mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left"><th>Titre</th><th>Catégorie</th><th>Marque</th><th>Modèle</th><th>Ville</th><th>Prix location</th><th>Prix vente</th><th>Statut</th><th>Détail</th></tr></thead><tbody>{items.map(e=><tr className="border-t" key={e._id}><td>{e.title}</td><td>{e.category}</td><td>{e.brand||'—'}</td><td>{e.model||'—'}</td><td>{e.city||'—'}</td><td>{e.rentalPricePerMonth||'N/A'} {e.currency}</td><td>{e.salePrice||'N/A'} {e.currency}</td><td><span className="badge bg-yellow-100 text-yellow-800">{e.status}</span></td><td><DownloadPdfButton path={`/api/reports/equipment/${e._id}/pdf`} filename={`DEL-fiche-engin-${e._id}.pdf`} /> <Link className="underline" href={`/equipment/${e._id}`}>Voir</Link></td></tr>)}</tbody></table></div>}</section>}
