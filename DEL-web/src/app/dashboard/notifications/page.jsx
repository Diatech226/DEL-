'use client';import Link from 'next/link';import {useEffect,useState} from 'react';import {getMyNotifications,markAllNotificationsAsRead,markNotificationAsRead} from '../../../lib/api';
const pc={LOW:'bg-slate-100 text-slate-700',NORMAL:'bg-blue-100 text-blue-700',HIGH:'bg-amber-100 text-amber-800',CRITICAL:'bg-red-100 text-red-700'};const fmt=d=>d?new Date(d).toLocaleString('fr-FR'):'—';
export default function NotificationsPage(){const [items,setItems]=useState([]),[unread,setUnread]=useState(0),[loading,setLoading]=useState(true),[error,setError]=useState(''),[ok,setOk]=useState('');const load=()=>getMyNotifications().then(r=>{setItems(r.data||[]);setUnread(r.unreadCount||0)}).catch(e=>setError(e.message)).finally(()=>setLoading(false));useEffect(()=>{load()},[]);async function read(id){setOk('');await markNotificationAsRead(id);setOk('Notification marquée comme lue.');load()}async function readAll(){setOk('');await markAllNotificationsAsRead();setOk('Toutes les notifications sont lues.');load()}if(loading)return <p>Chargement…</p>;return <section><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-4xl font-black">Notifications</h1><p className="mt-2 text-slate-600">Non lues : <b>{unread}</b></p></div><button onClick={readAll} className="btn-primary" disabled={!unread}>Tout marquer comme lu</button></div>{error&&<p className="alert-error mt-4">{error}</p>}{ok&&<p className="alert-success mt-4">{ok}</p>}{!items.length&&<div className="card mt-6">Aucune notification pour le moment.</div>}<div className="mt-6 grid gap-4">{items.map(n=><article key={n._id} className={`card border-l-4 ${n.isRead?'border-slate-200':'border-gold'}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-xl font-black text-coal">{n.title}</h2><p className="mt-1 text-slate-700">{n.message}</p></div><span className="rounded-full bg-slate-900 px-3 py-1 text-xs font-bold text-white">{n.isRead?'Lu':'Non lu'}</span></div><div className="mt-4 flex flex-wrap gap-2 text-xs font-bold"><span className="rounded-full bg-slate-100 px-3 py-1">{n.type}</span><span className={`rounded-full px-3 py-1 ${pc[n.priority]||pc.NORMAL}`}>{n.priority}</span><span className="rounded-full bg-slate-100 px-3 py-1">{fmt(n.createdAt)}</span></div><div className="mt-4 flex gap-3">{!n.isRead&&<button onClick={()=>read(n._id)} className="rounded-xl border px-4 py-2 font-bold">Marquer comme lu</button>}{n.actionUrl&&<Link className="btn-primary" href={n.actionUrl}>Ouvrir</Link>}</div></article>)}</div></section>}
