'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {login,setToken} from '../../lib/api';
export default function Login(){const r=useRouter();const [msg,setMsg]=useState('');async function submit(e){e.preventDefault();setMsg('');try{const {token}=await login(Object.fromEntries(new FormData(e.currentTarget).entries()));setToken(token);r.push('/dashboard')}catch(err){setMsg(err.message)}}return <section className="mx-auto max-w-xl px-6 py-12"><h1 className="text-4xl font-black">Connexion</h1><form onSubmit={submit} className="card mt-8 grid gap-4"><input className="input" name="identifier" placeholder="Email ou téléphone" required/><input className="input" name="password" type="password" placeholder="Mot de passe" required/><button className="btn bg-gold text-coal">Se connecter</button>{msg&&<p className="alert-error">{msg}</p>}</form></section>}
