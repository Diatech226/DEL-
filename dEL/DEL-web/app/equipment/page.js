import Link from 'next/link';
const apiUrl = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000/api';
async function getEquipment(){ try { const res = await fetch(`${apiUrl}/equipment`, { cache: 'no-store' }); return res.ok ? res.json() : []; } catch { return []; } }
export default async function EquipmentPage(){ const items = await getEquipment(); return <div className="mx-auto max-w-6xl px-6 py-10"><div className="flex items-center justify-between"><h1 className="text-3xl font-bold">Engins disponibles</h1><Link className="btn" href="/equipment/new">Déposer un engin</Link></div><div className="mt-8 grid gap-6 md:grid-cols-3">{items.length ? items.map((item) => <Link href={`/equipment/${item._id}`} className="card" key={item._id}><p className="text-sm font-semibold text-gold">{item.category}</p><h2 className="mt-2 text-xl font-bold">{item.title}</h2><p className="mt-2 text-slate-600">{item.location}</p><p className="mt-4 font-semibold">Statut: {item.status}</p></Link>) : <p className="text-slate-600">Aucun engin publié pour le moment.</p>}</div></div>; }
