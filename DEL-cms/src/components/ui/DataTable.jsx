import EmptyState from './EmptyState';export default function DataTable({columns=[],items=[]}){if(!items?.length)return <EmptyState/>;return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full min-w-[760px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr>{columns.map(c=><th key={c.key} className="p-3">{c.label}</th>)}</tr></thead><tbody>{items.map((item,i)=><tr className="border-t" key={item._id||i}>{columns.map(c=><td key={c.key} className="p-3">{c.render?c.render(item):item[c.key]||'—'}</td>)}</tr>)}</tbody></table></div>}
