import Button from './Button';export default function ConfirmModal({open,title='Confirmer',message,onConfirm,onCancel,loading}){if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><div className="max-w-md rounded-2xl bg-white p-6 shadow-xl"><h2 className="text-xl font-black">{title}</h2><p className="mt-2 text-slate-600">{message}</p><div className="mt-5 flex justify-end gap-2"><Button variant="outline" onClick={onCancel}>Annuler</Button><Button onClick={onConfirm} disabled={loading}>{loading?'Traitement…':'Confirmer'}</Button></div></div></div>}
