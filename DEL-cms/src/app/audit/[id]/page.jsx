'use client';
import Link from 'next/link';
import {useEffect,useState} from 'react';
import {useParams} from 'next/navigation';
import {getAuditLogById} from '../../../lib/api';
const formatDate=(d)=>d?new Date(d).toLocaleString('fr-FR'):'—';
const JsonBlock=({value})=><pre className="overflow-auto rounded bg-gray-950 p-4 text-xs text-gray-100">{JSON.stringify(value ?? null,null,2)}</pre>;
export default function AuditDetailPage(){const {id}=useParams();const [log,setLog]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{getAuditLogById(id).then(setLog).catch(e=>setError(e.message)).finally(()=>setLoading(false));},[id]);return <section><Link href="/audit" className="font-bold text-forest">← Retour audit</Link><h1 className="mt-4 text-4xl font-black">Détail audit</h1>{loading&&<p className="panel mt-6">Chargement...</p>}{error&&<p className="alert-error mt-6">{error}</p>}{log&&<div className="mt-6 grid gap-6 lg:grid-cols-2"><div className="panel grid gap-3"><p><b>Acteur :</b> {log.actorName||'Système'}</p><p><b>Rôle :</b> {log.actorRole||'—'}</p><p><b>Action :</b> {log.action}</p><p><b>Module :</b> {log.module}</p><p><b>EntityType :</b> {log.entityType||'—'}</p><p><b>EntityId :</b> {log.entityId||'—'}</p><p><b>EntityLabel :</b> {log.entityLabel||'—'}</p><p><b>Message :</b> {log.message||'—'}</p><p><b>IP :</b> {log.ipAddress||'—'}</p><p><b>User-Agent :</b> {log.userAgent||'—'}</p><p><b>Sévérité :</b> {log.severity}</p><p><b>Date :</b> {formatDate(log.createdAt)}</p></div><div className="grid gap-6"><div><h2 className="mb-2 text-xl font-black">Ancienne valeur</h2><JsonBlock value={log.oldValue}/></div><div><h2 className="mb-2 text-xl font-black">Nouvelle valeur</h2><JsonBlock value={log.newValue}/></div></div></div>}</section>}
