'use client';import {useEffect,useState} from 'react';import {getProposalList,createProposal,updateProposalStatus} from '../../lib/api';
const statuses=['SENT','ACCEPTED','REJECTED','EXPIRED'];const money=(v,c)=>v?`${Number(v).toLocaleString()} ${c||''}`:'N/A';
export default function Page(){const [items,setItems]=useState([]),[loading,setLoading]=useState(true),[msg,setMsg]=useState(''),[error,setError]=useState('');const load=()=>getProposalList().then(setItems);useEffect(()=>{load().catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);async function submit(e){e.preventDefault();const form=e.currentTarget;const f=new FormData(form);const data=Object.fromEntries(f.entries());data.equipmentIds=(data.equipmentIds||'').split(',').map(x=>x.trim()).filter(Boolean);data.ownerNames=(data.ownerNames||'').split(',').map(x=>x.trim()).filter(Boolean);data.finalPrice=Number(data.finalPrice);if(data.durationMonths)data.durationMonths=Number(data.durationMonths);else delete data.durationMonths;try{await createProposal(data);setMsg('Proposition créée.');form.reset();await load()}catch(err){setError(err.message)}}async function onStatus(id,status){try{await updateProposalStatus(id,status);setMsg(`Statut mis à jour : ${status}`);await load()}catch(e){setError(e.message)}}return <section><h1 className="text-3xl font-black">Propositions</h1><form onSubmit={submit} className="panel mt-6 grid gap-4 md:grid-cols-2">{['requestId','equipmentIds','companyName','ownerNames','title','finalPrice','currency','durationMonths'].map(n=><input className="input" name={n} placeholder={n} required={['requestId','title','finalPrice','currency'].includes(n)} key={n}/>)}<textarea className="input md:col-span-2" name="conditions" placeholder="conditions"/><button className="btn bg-gold text-coal">Créer</button></form>{msg&&<p className="alert-success mt-4">{msg}</p>}{error&&<p className="alert-error mt-4">{error}</p>}{loading&&<p className="panel mt-6">Chargement...</p>}<div className="panel mt-6 overflow-x-auto"><table className="w-full text-sm"><thead><tr className="text-left"><th>Titre</th><th>Entreprise</th><th>Propriétaires</th><th>Engins</th><th>Prix final</th><th>Durée</th><th>Statut</th><th>Conditions</th><th>Actions</th></tr></thead><tbody>{items.map(p=><tr className="border-t" key={p._id}><td>{p.title}</td><td>{p.companyName||'N/A'}</td><td>{(p.ownerNames||[]).join(', ')||'N/A'}</td><td>{p.equipmentIds?.length||0}</td><td>{money(p.finalPrice,p.currency)}</td><td>{p.durationMonths?`${p.durationMonths} mois`:'N/A'}</td><td><span className="badge bg-green-100 text-green-800">{p.status}</span></td><td>{p.conditions||'N/A'}</td><td className="flex flex-wrap gap-2 py-2">{statuses.map(s=><button className="btn bg-gray-100" onClick={()=>onStatus(p._id,s)} key={s}>{s}</button>)}</td></tr>)}</tbody></table>{items.length===0&&<p className="py-4">Aucune proposition.</p>}</div></section>}
