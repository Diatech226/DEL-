'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {clearToken,login,setToken} from '../../lib/api';
export default function CmsLogin(){const r=useRouter();const [msg,setMsg]=useState('');async function submit(e){e.preventDefault();setMsg('');try{const {user,token}=await login(Object.fromEntries(new FormData(e.currentTarget).entries()));if(user.role!=='ADMIN'){clearToken();setMsg('Accès réservé à l’administration DEL');return}setToken(token);r.push('/')}catch(err){setMsg(err.message)}}return <section className="mx-auto max-w-xl px-6 py-12"><h1 className="text-4xl font-black">Connexion admin DEL</h1><form onSubmit={submit} className="card mt-8 grid gap-4"><input className="input" name="identifier" placeholder="Email ou téléphone" required/><input className="input" name="password" type="password" placeholder="Mot de passe" required/><button className="btn bg-gold text-coal">Se connecter</button>{msg&&<p className="alert-error">{msg}</p>}</form></section>}
